/**
 * CLI module for stackdiff.
 * Provides argument parsing and runner utilities for the command-line interface.
 */
export { parseArgs } from './args';
export { run, main } from './runner';
export type { RunResult } from './runner';
